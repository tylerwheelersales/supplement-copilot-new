Milestone 1 – Tasks:

1. ✅ Set up backend project (Node + Express).
2. ✅ Create database schema for users, supplements, intake_logs.
3. ✅ Implement CRUD endpoints for supplements and intake logs.
4. Set up a basic React Native (Expo) app.
5. Create screens to list supplements and log intake.
6. Add photo capture/upload flow in the app (camera + image picker).
7. Create backend endpoint that accepts an image and calls Claude vision API to extract supplement details.
8. Return parsed fields to the app and pre-fill the add-supplement form for user review before saving.